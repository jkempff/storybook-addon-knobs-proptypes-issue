# Install/Run:

```bash
git clone git@github.com:jkempff/storybook-addon-knobs-proptypes-issue.git;

cd storybook-addon-knobs-proptypes-issue;

nvm use # optional, to use node v7.9.0
npm install && npm run storybook;
```
